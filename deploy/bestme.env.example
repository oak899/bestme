# Copy to /opt/bestme/bestme.env on the server (never commit secrets).
OPENAI_API_KEY=sk-...
# Optional: set to enable JWT auth on all /api/* routes (except /api/auth/*)
JWT_SECRET=change-me-to-a-long-random-string
